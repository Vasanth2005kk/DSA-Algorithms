# (Traversal Operation) ==> Visit all elements one by one

# Step 1: Create an array
array = [1, 2, 3, 4, 5]

# Step 2: Traverse using index
for index in range(len(array)):
    print("Element at index", index, ":", array[index])

# Time Complexity:
# O(n) → loop runs for all elements